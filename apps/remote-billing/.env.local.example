# OnePortal Billing - Authentication Configuration
# Copy this file to .env.local and replace with your actual Azure AD values

# Azure AD Application (Client) ID for Billing app
# MUST be different from Shell client ID for token isolation
# Each app needs its own Azure AD app registration
VITE_AUTH_CLIENT_ID=your-billing-client-id-guid-here

# Azure AD Tenant Authority (same as Shell)
VITE_AUTH_AUTHORITY=https://login.microsoftonline.com/common

# Redirect URI after authentication
# Must match exactly with Azure AD app registration
#
# SWA CLI + Docker (unified origin, app-specific path):
VITE_AUTH_REDIRECT_URI=http://localhost:4280/apps/billing/auth/callback
#
# Vite Dev Server (separate port):
# VITE_AUTH_REDIRECT_URI=http://localhost:5001/auth/callback
#
# Production: https://oneportal.azurestaticapps.net/apps/billing/auth/callback

# Post-logout redirect URI (optional)
# SWA CLI + Docker:
VITE_AUTH_POST_LOGOUT_REDIRECT_URI=http://localhost:4280/apps/billing/
# Vite Dev: http://localhost:5001/

# Default scopes for Billing (comma-separated)
# Add any additional API scopes needed for billing features
VITE_AUTH_SCOPES=User.Read

# App name for telemetry and logging
VITE_AUTH_APP_NAME=billing
