# OnePortal Reports - Authentication Configuration
# Copy this file to .env.local and replace with your actual Azure AD values

# Azure AD Application (Client) ID for Reports app
# MUST be different from Shell and Billing client IDs for token isolation
VITE_AUTH_CLIENT_ID=your-reports-client-id-guid-here

# Azure AD Tenant Authority (same as Shell and Billing)
VITE_AUTH_AUTHORITY=https://login.microsoftonline.com/common

# Redirect URI after authentication
# Must match exactly with Azure AD app registration
# Development:
VITE_AUTH_REDIRECT_URI=http://localhost:5002/auth/callback
# Production: https://oneportal.azurestaticapps.net/reports/auth/callback

# Post-logout redirect URI (optional)
VITE_AUTH_POST_LOGOUT_REDIRECT_URI=http://localhost:5002/

# Default scopes for Reports (comma-separated)
# Add any additional API scopes needed for reports features
VITE_AUTH_SCOPES=User.Read

# App name for telemetry and logging
VITE_AUTH_APP_NAME=reports
