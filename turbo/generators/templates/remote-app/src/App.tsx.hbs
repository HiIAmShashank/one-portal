import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@one-portal/ui';

/**
 * Main App component for the {{ displayName }} remote application
 * This component is exported via Module Federation and loaded by the shell
 * 
 * Requirements: FR-003 (Remote Application Integration)
 */
export default function App() {
  return (
    <div className="p-6">
      <Card>
        <CardHeader>
          <CardTitle>{{ displayName }}</CardTitle>
          <CardDescription>
            {{ description }}
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">
            This is a placeholder. Add your application content here.
          </p>
          
          {/* Example content */}
          <div className="mt-6 space-y-4">
            <div className="rounded-lg border p-4">
              <h3 className="font-semibold mb-2">Getting Started</h3>
              <p className="text-sm text-muted-foreground">
                Start building your features in this App.tsx file.
                Use components from @one-portal/ui for consistent styling.
              </p>
            </div>
            
            <div className="rounded-lg border p-4">
              <h3 className="font-semibold mb-2">Available Tools</h3>
              <ul className="text-sm text-muted-foreground list-disc list-inside space-y-1">
                <li>shadcn/ui components from @one-portal/ui</li>
                <li>Tailwind CSS for styling (with dark mode support)</li>
                <li>TypeScript for type safety</li>
                <li>TanStack Query (available via shell)</li>
              </ul>
            </div>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}
