# OnePortal {{ displayName }} - Authentication Configuration
# Generated by Turborepo generator

# Azure AD Application (Client) ID for {{ displayName }}
# MUST be different from other remote apps for token isolation
VITE_AUTH_CLIENT_ID={{ clientId }}

# Azure AD Tenant Authority
# Same across all OnePortal apps
VITE_AUTH_AUTHORITY={{ authority }}

# Redirect URI after authentication
# Must match exactly with Azure AD app registration
# Development:
VITE_AUTH_REDIRECT_URI={{ redirectUri }}
# Production: Update this for your production domain

# Post-logout redirect URI
# Where to redirect after user signs out
VITE_AUTH_POST_LOGOUT_REDIRECT_URI={{ postLogoutRedirectUri }}

# Default scopes for {{ displayName }} (comma-separated)
# Add any additional API scopes needed
VITE_AUTH_SCOPES={{ scopes }}

# App name for telemetry and logging
VITE_AUTH_APP_NAME={{ telemetryAppName }}

# ---------------------------------------------
# IMPORTANT: Security Notes
# ---------------------------------------------
# 1. Never commit this file to source control (.gitignore excludes it)
# 2. Each developer needs their own copy for local development
# 3. Production values set in Azure Static Web Apps configuration
# 4. Validate client ID matches Azure AD app registration
# 5. Ensure redirect URIs are registered in Azure AD
